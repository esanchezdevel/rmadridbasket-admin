
## Database Properties

spring.datasource.url = jdbc:mysql://localhost:3307/rmadridbasket?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = qwertyui

## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

spring.web.resources.add-mappings=true

# flyway configuration for database migrations. First time must be enabled=false for let jpa to create the schema
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate= true